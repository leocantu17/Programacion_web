function saludar(nombre) {
  // Define una función llamada 'saludar' que recibe un parámetro 'nombre'
  return "Hola, " + nombre + "!";
  // La función devuelve una cadena que saluda al nombre proporcionado
}

let mensaje = saludar("Ana");
// Llama a la función 'saludar' pasando "Ana" como argumento y guarda el resultado en la variable 'mensaje'

console.log(mensaje);
// Imprime en la consola el contenido de la variable 'mensaje', que será: "Hola, Ana!"
